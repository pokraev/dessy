---
phase: 01-canvas-foundation
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useKeyboardShortcuts.ts
autonomous: true
gap_closure: true
requirements:
  - CANV-07
  - UXSH-06

must_haves:
  truths:
    - "Pressing Ctrl+S in the editor triggers a project save identical to clicking the Header Save button"
  artifacts:
    - path: "src/hooks/useKeyboardShortcuts.ts"
      provides: "Ctrl+S keyboard shortcut wired to triggerSave"
      contains: "triggerSave"
  key_links:
    - from: "src/hooks/useKeyboardShortcuts.ts"
      to: "src/stores/canvasStore.ts"
      via: "useCanvasStore.getState().triggerSave?.()"
      pattern: "triggerSave"
---

<objective>
Wire the Ctrl+S keyboard shortcut to the existing triggerSave callback in canvasStore.

Purpose: Close the single verification gap — Ctrl+S is currently a no-op with an unresolved TODO from Plan 05/06 handoff.
Output: Working Ctrl+S save shortcut.
</objective>

<execution_context>
@/Users/pokraev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pokraev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Ctrl+S to triggerSave in useKeyboardShortcuts</name>
  <files>src/hooks/useKeyboardShortcuts.ts</files>
  <read_first>
    - src/hooks/useKeyboardShortcuts.ts (current file with the TODO on line 138)
    - src/stores/canvasStore.ts (confirms triggerSave is available via getState())
  </read_first>
  <action>
In src/hooks/useKeyboardShortcuts.ts, find the Ctrl+S handler block (around lines 135-140). Replace the TODO comment with a call to the existing triggerSave callback:

Replace:
```typescript
if (key === 's') {
  e.preventDefault();
  // TODO: wired in Plan 06
  return;
}
```

With:
```typescript
if (key === 's') {
  e.preventDefault();
  useCanvasStore.getState().triggerSave?.();
  return;
}
```

Ensure `useCanvasStore` is already imported at the top of the file. If not, add:
```typescript
import { useCanvasStore } from '@/stores/canvasStore';
```
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && grep -n "triggerSave" src/hooks/useKeyboardShortcuts.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "triggerSave" src/hooks/useKeyboardShortcuts.ts returns at least one match containing "useCanvasStore.getState().triggerSave?.()"
    - grep "TODO.*wired" src/hooks/useKeyboardShortcuts.ts returns zero matches (TODO comment removed)
    - The file still contains e.preventDefault() in the Ctrl+S block
    - npx tsc --noEmit exits with code 0 (no type errors introduced)
  </acceptance_criteria>
  <done>Ctrl+S keyboard shortcut calls triggerSave from canvasStore, TODO comment removed, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
1. `grep -n "triggerSave" src/hooks/useKeyboardShortcuts.ts` shows the wired call
2. `grep -c "TODO" src/hooks/useKeyboardShortcuts.ts` returns 0 (no remaining TODOs)
3. `npx tsc --noEmit` passes with no errors
4. `npm test` passes (all existing tests still green)
</verification>

<success_criteria>
Ctrl+S keyboard shortcut invokes the same save function as the Header Save button. The single verification gap from 01-VERIFICATION.md is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-foundation/01-07-SUMMARY.md`
</output>
