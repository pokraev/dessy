---
phase: 01-canvas-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - jest.config.ts
  - jest.setup.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/editor/[projectId]/page.tsx
  - src/types/project.ts
  - src/types/elements.ts
  - src/types/brand.ts
  - src/constants/formats.ts
  - src/constants/shortcuts.ts
  - src/lib/units.ts
  - src/stores/canvasStore.ts
  - src/stores/editorStore.ts
  - src/stores/projectStore.ts
  - src/stores/brandStore.ts
  - src/lib/__tests__/units.test.ts
  - src/constants/__tests__/formats.test.ts
autonomous: true
requirements:
  - CANV-01
  - UXSH-01

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "Navigating to /editor/test-id renders a dark-themed page"
    - "Unit tests for mm/px conversion and format constants pass"
    - "All TypeScript types compile without errors"
  artifacts:
    - path: "src/lib/units.ts"
      provides: "mm-to-px and px-to-mm conversion functions"
      exports: ["mmToPx", "pxToMm"]
    - path: "src/constants/formats.ts"
      provides: "Leaflet format presets with mm dimensions"
      exports: ["FORMATS", "LeafletFormat"]
    - path: "src/types/project.ts"
      provides: "Project, Page, ProjectMeta types"
      exports: ["Project", "Page", "ProjectMeta", "LeafletFormatId"]
    - path: "src/types/elements.ts"
      provides: "Element type definitions for all 5 element types"
      exports: ["DesignElement", "TextFrame", "ImageFrame", "ShapeElement", "ColorBlock", "GroupElement"]
    - path: "src/stores/canvasStore.ts"
      provides: "Canvas state store"
      exports: ["useCanvasStore"]
    - path: "src/stores/projectStore.ts"
      provides: "Project state store"
      exports: ["useProjectStore"]
  key_links:
    - from: "src/constants/formats.ts"
      to: "src/lib/units.ts"
      via: "imports mmToPx for computing pixel dimensions"
      pattern: "import.*mmToPx.*from.*units"
    - from: "src/stores/canvasStore.ts"
      to: "src/types/elements.ts"
      via: "uses element types for selection state"
      pattern: "import.*from.*types/elements"
---

<objective>
Scaffold the entire Next.js 15 project from scratch, establish the type system, unit conversion, format presets, Zustand store skeletons, dark theme, test framework, and routing.

Purpose: Everything in Phase 1 depends on these foundations. Types, stores, and the unit system must exist before the canvas or UI can be built.
Output: A running Next.js dev server with dark theme, all TypeScript types, Zustand stores, unit conversion library, format constants, and passing unit tests.
</objective>

<execution_context>
@/Users/pokraev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pokraev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canvas-foundation/01-CONTEXT.md
@.planning/phases/01-canvas-foundation/01-RESEARCH.md
@.planning/phases/01-canvas-foundation/01-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project, install dependencies, configure dark theme and routing</name>
  <files>
    package.json, tsconfig.json, next.config.ts, src/app/globals.css, src/app/layout.tsx,
    src/app/page.tsx, src/app/editor/[projectId]/page.tsx, jest.config.ts, jest.setup.ts
  </files>
  <read_first>
    CLAUDE.md (tech stack, design system colors, build instructions),
    .planning/phases/01-canvas-foundation/01-RESEARCH.md (installation commands, next.config.ts webpack externals, dark theme CSS pattern),
    .planning/phases/01-canvas-foundation/01-UI-SPEC.md (design system section, color table, typography table)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack` (use current directory since it exists).
       If the directory is not empty, use `--yes` or create in a temp dir and move files. The project name is "dessy".

    2. Install all Phase 1 dependencies:
       ```
       npm install fabric zustand motion zundo idb immer react-hot-toast lucide-react
       npm install -D jest @types/jest jest-environment-jsdom ts-jest @testing-library/react @testing-library/jest-dom
       ```
       Do NOT install `@types/fabric` (Fabric.js 7 ships its own types).
       Do NOT install `fabric-guideline-plugin` yet (Plan 04 verifies compatibility first).

    3. Configure `next.config.ts` — add webpack externals for canvas:
       ```typescript
       import type { NextConfig } from 'next';
       const nextConfig: NextConfig = {
         webpack: (config) => {
           config.externals = [...(config.externals || []), { canvas: 'canvas' }];
           return config;
         },
       };
       export default nextConfig;
       ```

    4. Set up `src/app/globals.css` with Tailwind CSS 4 dark theme:
       ```css
       @import "tailwindcss";

       @custom-variant dark (&:where(.dark, .dark *));

       @theme {
         --color-bg: #0a0a0a;
         --color-surface: #141414;
         --color-surface-raised: #1e1e1e;
         --color-border: #2a2a2a;
         --color-text-primary: #f5f5f5;
         --color-text-secondary: #888888;
         --color-accent: #6366f1;
         --color-accent-hover: #818cf8;
         --color-success: #22c55e;
         --color-warning: #eab308;
         --color-danger: #ef4444;
       }
       ```
       Load Inter and JetBrains Mono from Google Fonts in layout.tsx via `next/font/google`.

    5. Set up `src/app/layout.tsx`:
       - Import Inter and JetBrains Mono via `next/font/google`
       - Apply `dark` class to `<html>` element
       - Set `className` with Inter as default font
       - Set CSS variable `--font-mono` for JetBrains Mono
       - Set metadata: title "Dessy - Leaflet Factory", description "AI-powered leaflet design tool"
       - Background: `bg-bg` (#0a0a0a)

    6. Create `src/app/page.tsx` — minimal dashboard stub:
       ```tsx
       export default function DashboardPage() {
         return (
           <div className="flex items-center justify-center h-screen bg-bg text-text-primary">
             <p className="text-[13px]">Dashboard — coming in Phase 4</p>
           </div>
         );
       }
       ```

    7. Create `src/app/editor/[projectId]/page.tsx` — client-side editor stub:
       ```tsx
       'use client';
       export default function EditorPage() {
         return (
           <div className="flex items-center justify-center h-screen bg-bg text-text-primary">
             <p className="text-[13px]">Editor canvas loading...</p>
           </div>
         );
       }
       ```

    8. Configure `jest.config.ts`:
       ```typescript
       import type { Config } from 'jest';
       const config: Config = {
         preset: 'ts-jest',
         testEnvironment: 'jsdom',
         moduleNameMapper: { '^@/(.*)$': '<rootDir>/src/$1' },
         setupFilesAfterEnv: ['<rootDir>/jest.setup.ts'],
         transform: { '^.+\\.tsx?$': ['ts-jest', { tsconfig: 'tsconfig.json' }] },
       };
       export default config;
       ```

    9. Create `jest.setup.ts`:
       ```typescript
       import '@testing-library/jest-dom';
       ```

    10. Verify: `npm run dev` starts without errors. `npm run build` succeeds.
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && npm run dev -- --port 3099 &amp; sleep 5 &amp;&amp; curl -s -o /dev/null -w "%{http_code}" http://localhost:3099/editor/test-id &amp;&amp; kill %1</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "fabric", "zustand", "motion", "zundo", "idb", "immer", "react-hot-toast", "lucide-react" in dependencies
    - package.json does NOT contain "@types/fabric"
    - next.config.ts contains `externals` with `{ canvas: 'canvas' }`
    - src/app/globals.css contains `@custom-variant dark`
    - src/app/globals.css contains `--color-bg: #0a0a0a` and `--color-accent: #6366f1`
    - src/app/layout.tsx contains `className="dark"` on html element
    - src/app/layout.tsx imports Inter and JetBrains Mono from `next/font/google`
    - src/app/editor/[projectId]/page.tsx exists with `'use client'` directive
    - jest.config.ts exists with `testEnvironment: 'jsdom'`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Next.js 15 app scaffolded with all dependencies, dark theme, routing, and test framework configured. Dev server starts, /editor/[projectId] route renders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create type system, unit conversion, format constants, keyboard shortcut constants, and Zustand store skeletons</name>
  <files>
    src/types/project.ts, src/types/elements.ts, src/types/brand.ts,
    src/constants/formats.ts, src/constants/shortcuts.ts,
    src/lib/units.ts,
    src/stores/canvasStore.ts, src/stores/editorStore.ts, src/stores/projectStore.ts, src/stores/brandStore.ts,
    src/lib/__tests__/units.test.ts, src/constants/__tests__/formats.test.ts
  </files>
  <read_first>
    CLAUDE.md (Zustand ProjectState interface, element types, format options, keyboard shortcuts),
    .planning/phases/01-canvas-foundation/01-RESEARCH.md (units.ts pattern, format presets, custom properties list, store structure),
    .planning/phases/01-canvas-foundation/01-CONTEXT.md (element creation decisions, undo granularity)
  </read_first>
  <behavior>
    - Test 1: `mmToPx(210, 300)` returns approximately 2480.3 (A4 width at 300dpi)
    - Test 2: `pxToMm(595.28, 72)` returns approximately 210 (A4 width at screen dpi)
    - Test 3: `FORMATS.A4.widthMm === 210` and `FORMATS.A4.heightMm === 297`
    - Test 4: All format presets have bleedMm === 3
  </behavior>
  <action>
    1. Create `src/types/project.ts`:
       ```typescript
       export type LeafletFormatId = 'A4' | 'A5' | 'DL' | 'bifold' | 'trifold' | 'custom';

       export interface ProjectMeta {
         id: string;
         name: string;
         format: LeafletFormatId;
         createdAt: string;  // ISO 8601
         updatedAt: string;
       }

       export interface Page {
         id: string;
         elements: string[];  // element IDs, order = z-order
         background: string;  // hex color, default '#FFFFFF'
       }

       export interface Project {
         meta: ProjectMeta;
         pages: Page[];
         currentPageIndex: number;
         brandColors: string[];  // hex values
       }
       ```

    2. Create `src/types/elements.ts`:
       ```typescript
       export type ElementType = 'text' | 'image' | 'shape' | 'colorBlock' | 'group';
       export type ShapeKind = 'rect' | 'circle' | 'line';
       export type ImageFitMode = 'fill' | 'fit' | 'stretch';

       export interface BaseElement {
         id: string;
         customType: ElementType;
         name: string;
         locked: boolean;
         visible: boolean;
       }

       export interface TextFrame extends BaseElement {
         customType: 'text';
         text: string;
         fontFamily: string;
         fontSize: number;      // pt
         fontWeight: number;
         fill: string;          // hex
         textAlign: 'left' | 'center' | 'right' | 'justify';
       }

       export interface ImageFrame extends BaseElement {
         customType: 'image';
         imageId: string | null;  // UUID referencing IndexedDB
         fitMode: ImageFitMode;
       }

       export interface ShapeElement extends BaseElement {
         customType: 'shape';
         shapeKind: ShapeKind;
         fill: string;
         stroke: string;
         strokeWidth: number;
         rx: number;  // corner radius
         ry: number;
       }

       export interface ColorBlock extends BaseElement {
         customType: 'colorBlock';
         fill: string;          // hex or gradient string
         gradientType?: 'linear' | 'radial';
         gradientStops?: Array<{ offset: number; color: string }>;
       }

       export interface GroupElement extends BaseElement {
         customType: 'group';
         childIds: string[];
       }

       export type DesignElement = TextFrame | ImageFrame | ShapeElement | ColorBlock | GroupElement;
       ```

    3. Create `src/types/brand.ts`:
       ```typescript
       export interface ColorSwatch {
         id: string;
         hex: string;
         name?: string;
       }

       export interface TypographyPreset {
         id: string;
         name: string;         // 'Headline' | 'Subhead' | 'Body' | 'Caption' | 'CTA'
         fontFamily: string;
         fontSize: number;     // pt
         fontWeight: number;
         lineHeight: number;
         letterSpacing: number;
         color: string;        // hex
       }
       ```

    4. Create `src/lib/units.ts`:
       ```typescript
       export const SCREEN_DPI = 72;

       export const mmToPx = (mm: number, dpi = SCREEN_DPI): number =>
         (mm * dpi) / 25.4;

       export const pxToMm = (px: number, dpi = SCREEN_DPI): number =>
         (px * 25.4) / dpi;
       ```

    5. Create `src/constants/formats.ts`:
       ```typescript
       import { mmToPx } from '@/lib/units';

       export interface FormatDefinition {
         widthMm: number;
         heightMm: number;
         bleedMm: number;
         pages: number;
         label: string;
       }

       export const FORMATS: Record<string, FormatDefinition> = {
         A4:      { widthMm: 210, heightMm: 297, bleedMm: 3, pages: 1, label: 'A4 Single' },
         A5:      { widthMm: 148, heightMm: 210, bleedMm: 3, pages: 1, label: 'A5' },
         DL:      { widthMm: 99,  heightMm: 210, bleedMm: 3, pages: 1, label: 'DL' },
         bifold:  { widthMm: 420, heightMm: 297, bleedMm: 3, pages: 2, label: 'A4 Bifold' },
         trifold: { widthMm: 630, heightMm: 297, bleedMm: 3, pages: 3, label: 'A4 Trifold' },
       } as const;

       export function getFormatPixelDimensions(formatId: string, dpi?: number) {
         const fmt = FORMATS[formatId];
         if (!fmt) throw new Error(`Unknown format: ${formatId}`);
         return {
           widthPx: mmToPx(fmt.widthMm, dpi),
           heightPx: mmToPx(fmt.heightMm, dpi),
           bleedPx: mmToPx(fmt.bleedMm, dpi),
         };
       }
       ```

    6. Create `src/constants/shortcuts.ts`:
       ```typescript
       export interface ShortcutDef {
         key: string;
         modifiers?: ('ctrl' | 'shift' | 'alt')[];
         label: string;
         description: string;
         section: 'Tools' | 'Canvas' | 'Edit' | 'File';
       }

       export const SHORTCUTS: ShortcutDef[] = [
         // Tools
         { key: 'v', label: 'V', description: 'Select tool', section: 'Tools' },
         { key: 't', label: 'T', description: 'Text tool', section: 'Tools' },
         { key: 'r', label: 'R', description: 'Rectangle tool', section: 'Tools' },
         { key: 'c', label: 'C', description: 'Circle tool', section: 'Tools' },
         { key: 'l', label: 'L', description: 'Line tool', section: 'Tools' },
         { key: 'i', label: 'I', description: 'Image tool', section: 'Tools' },
         { key: 'h', label: 'H', description: 'Pan tool', section: 'Tools' },
         // Canvas
         { key: '+', modifiers: ['ctrl'], label: 'Ctrl +', description: 'Zoom in', section: 'Canvas' },
         { key: '-', modifiers: ['ctrl'], label: 'Ctrl -', description: 'Zoom out', section: 'Canvas' },
         { key: '0', modifiers: ['ctrl'], label: 'Ctrl 0', description: 'Zoom to fit', section: 'Canvas' },
         { key: '1', modifiers: ['ctrl'], label: 'Ctrl 1', description: 'Zoom to 100%', section: 'Canvas' },
         // Edit
         { key: 'z', modifiers: ['ctrl'], label: 'Ctrl Z', description: 'Undo', section: 'Edit' },
         { key: 'z', modifiers: ['ctrl', 'shift'], label: 'Ctrl Shift Z', description: 'Redo', section: 'Edit' },
         { key: 'c', modifiers: ['ctrl'], label: 'Ctrl C', description: 'Copy', section: 'Edit' },
         { key: 'v', modifiers: ['ctrl'], label: 'Ctrl V', description: 'Paste', section: 'Edit' },
         { key: 'Delete', label: 'Delete', description: 'Delete selected', section: 'Edit' },
         { key: 'Backspace', label: 'Backspace', description: 'Delete selected', section: 'Edit' },
         { key: 'ArrowUp', label: 'Arrow Up', description: 'Nudge up 1px', section: 'Edit' },
         { key: 'ArrowDown', label: 'Arrow Down', description: 'Nudge down 1px', section: 'Edit' },
         { key: 'ArrowLeft', label: 'Arrow Left', description: 'Nudge left 1px', section: 'Edit' },
         { key: 'ArrowRight', label: 'Arrow Right', description: 'Nudge right 1px', section: 'Edit' },
         { key: 'ArrowUp', modifiers: ['shift'], label: 'Shift Up', description: 'Nudge up 10px', section: 'Edit' },
         { key: 'ArrowDown', modifiers: ['shift'], label: 'Shift Down', description: 'Nudge down 10px', section: 'Edit' },
         { key: 'ArrowLeft', modifiers: ['shift'], label: 'Shift Left', description: 'Nudge left 10px', section: 'Edit' },
         { key: 'ArrowRight', modifiers: ['shift'], label: 'Shift Right', description: 'Nudge right 10px', section: 'Edit' },
         { key: 'g', modifiers: ['ctrl'], label: 'Ctrl G', description: 'Group selected', section: 'Edit' },
         { key: 'g', modifiers: ['ctrl', 'shift'], label: 'Ctrl Shift G', description: 'Ungroup', section: 'Edit' },
         // File
         { key: 's', modifiers: ['ctrl'], label: 'Ctrl S', description: 'Save project', section: 'File' },
         { key: '?', label: '?', description: 'Show keyboard shortcuts', section: 'File' },
       ];
       ```

    7. Create `src/stores/canvasStore.ts` — Zustand store for canvas state:
       ```typescript
       import { create } from 'zustand';

       export type ToolId = 'select' | 'text' | 'rect' | 'circle' | 'line' | 'image' | 'hand';

       interface CanvasState {
         activeTool: ToolId;
         zoom: number;
         selectedObjectIds: string[];
         canUndo: boolean;
         canRedo: boolean;
         viewportTransform: number[];
         // Actions
         setActiveTool: (tool: ToolId) => void;
         setZoom: (zoom: number) => void;
         setSelection: (ids: string[]) => void;
         setUndoRedo: (canUndo: boolean, canRedo: boolean) => void;
         setViewportTransform: (vt: number[]) => void;
       }

       export const useCanvasStore = create<CanvasState>((set) => ({
         activeTool: 'select',
         zoom: 1,
         selectedObjectIds: [],
         canUndo: false,
         canRedo: false,
         viewportTransform: [1, 0, 0, 1, 0, 0],
         setActiveTool: (tool) => set({ activeTool: tool }),
         setZoom: (zoom) => set({ zoom }),
         setSelection: (ids) => set({ selectedObjectIds: ids }),
         setUndoRedo: (canUndo, canRedo) => set({ canUndo, canRedo }),
         setViewportTransform: (vt) => set({ viewportTransform: vt }),
       }));
       ```

    8. Create `src/stores/editorStore.ts`:
       ```typescript
       import { create } from 'zustand';

       interface EditorState {
         showGrid: boolean;
         showRulers: boolean;
         showGuides: boolean;
         leftPanelOpen: boolean;
         rightPanelOpen: boolean;
         shortcutsModalOpen: boolean;
         toggleGrid: () => void;
         toggleRulers: () => void;
         toggleGuides: () => void;
         toggleLeftPanel: () => void;
         toggleRightPanel: () => void;
         setShortcutsModalOpen: (open: boolean) => void;
       }

       export const useEditorStore = create<EditorState>((set) => ({
         showGrid: false,
         showRulers: true,
         showGuides: true,
         leftPanelOpen: true,
         rightPanelOpen: true,
         shortcutsModalOpen: false,
         toggleGrid: () => set((s) => ({ showGrid: !s.showGrid })),
         toggleRulers: () => set((s) => ({ showRulers: !s.showRulers })),
         toggleGuides: () => set((s) => ({ showGuides: !s.showGuides })),
         toggleLeftPanel: () => set((s) => ({ leftPanelOpen: !s.leftPanelOpen })),
         toggleRightPanel: () => set((s) => ({ rightPanelOpen: !s.rightPanelOpen })),
         setShortcutsModalOpen: (open) => set({ shortcutsModalOpen: open }),
       }));
       ```

    9. Create `src/stores/projectStore.ts`:
       ```typescript
       import { create } from 'zustand';
       import type { Project, ProjectMeta, Page } from '@/types/project';

       interface ProjectState {
         currentProject: Project | null;
         projectList: ProjectMeta[];
         lastSaved: string | null;  // ISO timestamp
         isDirty: boolean;
         // Actions
         setCurrentProject: (project: Project) => void;
         setProjectList: (list: ProjectMeta[]) => void;
         setLastSaved: (date: Date) => void;
         markDirty: () => void;
         markClean: () => void;
         updateProjectName: (name: string) => void;
         getCurrentPage: () => Page | null;
       }

       export const useProjectStore = create<ProjectState>((set, get) => ({
         currentProject: null,
         projectList: [],
         lastSaved: null,
         isDirty: false,
         setCurrentProject: (project) => set({ currentProject: project }),
         setProjectList: (list) => set({ projectList: list }),
         setLastSaved: (date) => set({ lastSaved: date.toISOString(), isDirty: false }),
         markDirty: () => set({ isDirty: true }),
         markClean: () => set({ isDirty: false }),
         updateProjectName: (name) => set((s) => ({
           currentProject: s.currentProject
             ? { ...s.currentProject, meta: { ...s.currentProject.meta, name, updatedAt: new Date().toISOString() } }
             : null,
         })),
         getCurrentPage: () => {
           const { currentProject } = get();
           if (!currentProject) return null;
           return currentProject.pages[currentProject.currentPageIndex] ?? null;
         },
       }));
       ```

    10. Create `src/stores/brandStore.ts`:
        ```typescript
        import { create } from 'zustand';
        import type { ColorSwatch, TypographyPreset } from '@/types/brand';

        interface BrandState {
          brandColors: ColorSwatch[];
          typographyPresets: TypographyPreset[];
          setBrandColors: (colors: ColorSwatch[]) => void;
          setTypographyPresets: (presets: TypographyPreset[]) => void;
        }

        export const useBrandStore = create<BrandState>((set) => ({
          brandColors: [],
          typographyPresets: [],
          setBrandColors: (colors) => set({ brandColors: colors }),
          setTypographyPresets: (presets) => set({ typographyPresets: presets }),
        }));
        ```

    11. Create `src/lib/__tests__/units.test.ts`:
        ```typescript
        import { mmToPx, pxToMm, SCREEN_DPI } from '@/lib/units';

        describe('units', () => {
          test('mmToPx converts A4 width at 300dpi', () => {
            expect(mmToPx(210, 300)).toBeCloseTo(2480.31, 0);
          });
          test('mmToPx converts A4 width at screen dpi (72)', () => {
            expect(mmToPx(210, 72)).toBeCloseTo(595.28, 0);
          });
          test('pxToMm inverts mmToPx at screen dpi', () => {
            const px = mmToPx(148);
            expect(pxToMm(px)).toBeCloseTo(148, 2);
          });
          test('SCREEN_DPI is 72', () => {
            expect(SCREEN_DPI).toBe(72);
          });
        });
        ```

    12. Create `src/constants/__tests__/formats.test.ts`:
        ```typescript
        import { FORMATS, getFormatPixelDimensions } from '@/constants/formats';

        describe('formats', () => {
          test('A4 dimensions are 210x297mm', () => {
            expect(FORMATS.A4.widthMm).toBe(210);
            expect(FORMATS.A4.heightMm).toBe(297);
          });
          test('all formats have 3mm bleed', () => {
            Object.values(FORMATS).forEach((fmt) => {
              expect(fmt.bleedMm).toBe(3);
            });
          });
          test('bifold has 2 pages, trifold has 3', () => {
            expect(FORMATS.bifold.pages).toBe(2);
            expect(FORMATS.trifold.pages).toBe(3);
          });
          test('getFormatPixelDimensions computes correct A4 at 300dpi', () => {
            const dims = getFormatPixelDimensions('A4', 300);
            expect(dims.widthPx).toBeCloseTo(2480.31, 0);
            expect(dims.heightPx).toBeCloseTo(3507.87, 0);
          });
          test('getFormatPixelDimensions throws on unknown format', () => {
            expect(() => getFormatPixelDimensions('Z9')).toThrow('Unknown format');
          });
        });
        ```
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && npx jest --no-coverage --testPathPattern="units|formats" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/types/project.ts exports `Project`, `Page`, `ProjectMeta`, `LeafletFormatId`
    - src/types/elements.ts exports `DesignElement`, `TextFrame`, `ImageFrame`, `ShapeElement`, `ColorBlock`, `GroupElement`, `ElementType`
    - src/types/brand.ts exports `ColorSwatch`, `TypographyPreset`
    - src/lib/units.ts exports `mmToPx`, `pxToMm`, `SCREEN_DPI`
    - src/constants/formats.ts contains `FORMATS` with keys A4, A5, DL, bifold, trifold
    - src/constants/formats.ts: FORMATS.A4.widthMm === 210
    - src/constants/shortcuts.ts contains at least 25 shortcut definitions covering Tools, Canvas, Edit, File sections
    - src/stores/canvasStore.ts exports `useCanvasStore` with `activeTool`, `zoom`, `selectedObjectIds`, `setActiveTool`, `setZoom`
    - src/stores/editorStore.ts exports `useEditorStore` with `showGrid`, `showRulers`, `showGuides`, `shortcutsModalOpen`
    - src/stores/projectStore.ts exports `useProjectStore` with `currentProject`, `projectList`, `lastSaved`, `isDirty`
    - `npx jest --no-coverage --testPathPattern="units|formats"` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All TypeScript types, unit conversion, format constants, shortcut constants, and Zustand store skeletons exist. Unit tests for units and formats pass. TypeScript compilation succeeds with no errors.</done>
</task>

</tasks>

<verification>
- `npm run dev` starts Next.js on localhost:3000 without errors
- `npm run build` succeeds with exit code 0
- Navigating to http://localhost:3000 shows dark-themed dashboard stub
- Navigating to http://localhost:3000/editor/test-id shows dark-themed editor stub
- `npx jest --no-coverage` runs all tests green
- `npx tsc --noEmit` passes with no type errors
</verification>

<success_criteria>
- Running Next.js 15 app with dark theme, Inter + JetBrains Mono fonts
- All Phase 1 dependencies installed (fabric, zustand, motion, zundo, idb, immer, react-hot-toast, lucide-react)
- Complete type system for projects, elements, and brand
- Unit conversion (mm/px) and format presets with passing tests
- Keyboard shortcut constants covering all Phase 1 shortcuts
- Four Zustand stores (canvas, editor, project, brand) with all actions
- Jest + React Testing Library configured and running
</success_criteria>

<output>
After completion, create `.planning/phases/01-canvas-foundation/01-01-SUMMARY.md`
</output>
