---
phase: 05-dashboard-and-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/appStore.ts
  - src/lib/storage/thumbnailDb.ts
  - src/lib/storage/projectStorage.ts
  - src/lib/utils/relativeTime.ts
  - src/i18n/en.json
  - src/i18n/bg.json
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "appStore provides currentView (dashboard|editor) and activeProjectId state with openProject/goToDashboard actions"
    - "thumbnailDb stores and retrieves PNG blob thumbnails keyed by projectId from IndexedDB"
    - "projectStorage has duplicateProject and updateProjectName functions"
    - "relativeTime formats ISO dates as human-readable relative strings"
    - "All dashboard i18n keys exist in en.json and bg.json"
  artifacts:
    - path: "src/stores/appStore.ts"
      provides: "View switching state"
      exports: ["useAppStore"]
    - path: "src/lib/storage/thumbnailDb.ts"
      provides: "IndexedDB thumbnail CRUD"
      exports: ["saveThumbnail", "getThumbnail", "deleteThumbnail"]
    - path: "src/lib/storage/projectStorage.ts"
      provides: "Extended project CRUD"
      exports: ["duplicateProject", "updateProjectName"]
    - path: "src/lib/utils/relativeTime.ts"
      provides: "Relative time formatting"
      exports: ["relativeTime"]
    - path: "src/i18n/en.json"
      provides: "English dashboard strings"
      contains: "dashboard.newLeaflet"
  key_links:
    - from: "src/stores/appStore.ts"
      to: "zustand"
      via: "create from zustand"
      pattern: "create<AppState>"
    - from: "src/lib/storage/thumbnailDb.ts"
      to: "idb"
      via: "openDB from idb"
      pattern: "openDB.*dessy-thumbnails"
---

<objective>
Create all foundation modules for the dashboard phase: appStore for view switching, thumbnailDb for IndexedDB thumbnail storage, projectStorage extensions (duplicate, rename), relativeTime utility, and all i18n strings.

Purpose: These are the data layer and utilities that every dashboard UI component depends on. Building them first ensures Plan 02 and 03 can execute in parallel without blockers.
Output: 6 new/modified files providing the complete non-UI foundation for the dashboard.
</objective>

<execution_context>
@/Users/pokraev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pokraev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-and-templates/05-CONTEXT.md
@.planning/phases/05-dashboard-and-templates/05-RESEARCH.md
@.planning/phases/05-dashboard-and-templates/05-UI-SPEC.md

<interfaces>
<!-- Existing types/APIs the executor needs -->

From src/types/project.ts:
```typescript
export type LeafletFormatId = 'A4' | 'A5' | 'DL' | 'bifold' | 'trifold' | 'custom';
export interface ProjectMeta {
  id: string;
  name: string;
  format: LeafletFormatId;
  createdAt: string;
  updatedAt: string;
}
```

From src/lib/storage/projectStorage.ts:
```typescript
const PROJECT_PREFIX = 'dessy-project-';
const PROJECT_LIST_KEY = 'dessy-project-list';
export function saveProject(projectId: string, data: { meta: ProjectMeta; canvasJSON: object; pageData: object; brandData?: object }): SaveResult;
export function loadProject(projectId: string): { meta: ProjectMeta; canvasJSON: object; pageData: object; brandData?: object } | null;
export function listProjects(): ProjectMeta[];
export function deleteProject(projectId: string): void;
```

From src/lib/storage/imageDb.ts (pattern to follow for thumbnailDb):
```typescript
import { openDB } from 'idb';
const DB_NAME = 'dessy-images';
const STORE_NAME = 'images';
const dbPromise = typeof window !== 'undefined'
  ? openDB(DB_NAME, 1, { upgrade(db) { db.createObjectStore(STORE_NAME); } })
  : null;
```

From src/stores/editorStore.ts (Zustand store pattern):
```typescript
import { create } from 'zustand';
export const useEditorStore = create<EditorState>((set) => ({ ... }));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create appStore, thumbnailDb, and relativeTime utility</name>
  <files>src/stores/appStore.ts, src/lib/storage/thumbnailDb.ts, src/lib/utils/relativeTime.ts</files>
  <read_first>
    - src/stores/editorStore.ts (Zustand store pattern to follow)
    - src/lib/storage/imageDb.ts (IndexedDB pattern to follow exactly)
    - src/types/project.ts (LeafletFormatId type)
  </read_first>
  <action>
Create three new files:

**src/stores/appStore.ts:**
```typescript
import { create } from 'zustand';

interface AppState {
  currentView: 'dashboard' | 'editor';
  activeProjectId: string | null;
  openProject: (projectId: string) => void;
  goToDashboard: () => void;
  createNewProject: (projectId: string) => void;
}

export const useAppStore = create<AppState>((set) => ({
  currentView: 'dashboard',
  activeProjectId: null,
  openProject: (projectId) => set({ currentView: 'editor', activeProjectId: projectId }),
  goToDashboard: () => set({ currentView: 'dashboard', activeProjectId: null }),
  createNewProject: (projectId) => set({ currentView: 'editor', activeProjectId: projectId }),
}));
```

**src/lib/storage/thumbnailDb.ts:**
Follow the exact same pattern as `imageDb.ts`. DB_NAME = `'dessy-thumbnails'`, STORE_NAME = `'thumbnails'`.

Three exported functions:
- `saveThumbnail(projectId: string, dataUrl: string): Promise<void>` — converts dataUrl to blob via `fetch(dataUrl).then(r => r.blob())`, stores blob under projectId key
- `getThumbnail(projectId: string): Promise<string | null>` — retrieves blob, returns `URL.createObjectURL(blob)` or null
- `deleteThumbnail(projectId: string): Promise<void>` — deletes blob by key

Guard `dbPromise` with `typeof window !== 'undefined'` check (same as imageDb).

**src/lib/utils/relativeTime.ts:**
Single exported function `relativeTime(isoDate: string): string` using `Intl.RelativeTimeFormat('en', { numeric: 'auto' })`:
- diff = Date.now() - new Date(isoDate).getTime()
- seconds < 60 → format as seconds
- seconds < 3600 → format as minutes
- seconds < 86400 → format as hours
- else → format as days
  </action>
  <verify>
    <automated>grep -n "export const useAppStore" src/stores/appStore.ts && grep -n "saveThumbnail\|getThumbnail\|deleteThumbnail" src/lib/storage/thumbnailDb.ts && grep -n "export function relativeTime" src/lib/utils/relativeTime.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/appStore.ts contains `currentView: 'dashboard' | 'editor'`
    - src/stores/appStore.ts contains `activeProjectId: string | null`
    - src/stores/appStore.ts contains `openProject` and `goToDashboard` and `createNewProject`
    - src/lib/storage/thumbnailDb.ts contains `dessy-thumbnails` as DB_NAME
    - src/lib/storage/thumbnailDb.ts contains `export async function saveThumbnail`
    - src/lib/storage/thumbnailDb.ts contains `export async function getThumbnail`
    - src/lib/storage/thumbnailDb.ts contains `export async function deleteThumbnail`
    - src/lib/storage/thumbnailDb.ts contains `typeof window !== 'undefined'` guard
    - src/lib/utils/relativeTime.ts contains `export function relativeTime`
    - src/lib/utils/relativeTime.ts contains `Intl.RelativeTimeFormat`
  </acceptance_criteria>
  <done>All three files exist with correct exports and patterns matching existing codebase conventions</done>
</task>

<task type="auto">
  <name>Task 2: Extend projectStorage with duplicateProject and updateProjectName, add all i18n dashboard strings</name>
  <files>src/lib/storage/projectStorage.ts, src/i18n/en.json, src/i18n/bg.json</files>
  <read_first>
    - src/lib/storage/projectStorage.ts (current full file — must append to existing code)
    - src/i18n/en.json (full file — must add dashboard key block)
    - src/i18n/bg.json (full file — must add dashboard key block)
    - src/types/project.ts (ProjectMeta type)
  </read_first>
  <action>
**src/lib/storage/projectStorage.ts — ADD two new functions (do not modify existing functions):**

```typescript
export function duplicateProject(sourceId: string): string | null {
  const source = loadProject(sourceId);
  if (!source) return null;
  const newId = crypto.randomUUID();
  const newMeta: ProjectMeta = {
    ...source.meta,
    id: newId,
    name: `Copy of ${source.meta.name}`,
    createdAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  };
  saveProject(newId, { ...source, meta: newMeta });
  return newId;
}

export function updateProjectName(projectId: string, newName: string): boolean {
  const source = loadProject(projectId);
  if (!source) return false;
  const updatedMeta: ProjectMeta = {
    ...source.meta,
    name: newName,
    updatedAt: new Date().toISOString(),
  };
  saveProject(projectId, { ...source, meta: updatedMeta });
  return true;
}
```

**src/i18n/en.json — ADD `"dashboard"` key block** (insert at top level alongside existing `"welcome"`, `"app"`, `"header"` etc.):

```json
"dashboard": {
  "newLeaflet": "New Leaflet",
  "emptyHeading": "Create your first leaflet",
  "emptyBody": "Start blank or pick a template to get going in seconds.",
  "emptyCtaLabel": "New Leaflet",
  "recentProjects": "Recent Projects",
  "delete": "Delete",
  "rename": "Rename",
  "duplicate": "Duplicate",
  "deleteConfirm": "Delete \"{{name}}\"? This cannot be undone.",
  "deleteConfirmButton": "Delete",
  "cancel": "Keep Project",
  "newLeafletTitle": "New Leaflet",
  "tabBlank": "Blank",
  "tabTemplates": "Templates",
  "customWidthLabel": "Width (mm)",
  "customHeightLabel": "Height (mm)",
  "useTemplate": "Use This Template",
  "templateCategory": "Category",
  "templateFormat": "Format",
  "templatePages": "{{count}} page",
  "templatePages_plural": "{{count}} pages",
  "pageCount": "{{count}} page",
  "pageCount_plural": "{{count}} pages",
  "noThumbnail": "",
  "errorLoadProject": "Could not open project. The data may be corrupted.",
  "errorDeleteProject": "Could not delete project. Try again.",
  "successDuplicate": "Project duplicated."
}
```

**src/i18n/bg.json — ADD `"dashboard"` key block** with Bulgarian translations:

```json
"dashboard": {
  "newLeaflet": "Нова листовка",
  "emptyHeading": "Създайте първата си листовка",
  "emptyBody": "Започнете празен проект или изберете шаблон за бърз старт.",
  "emptyCtaLabel": "Нова листовка",
  "recentProjects": "Скорошни проекти",
  "delete": "Изтриване",
  "rename": "Преименуване",
  "duplicate": "Дублиране",
  "deleteConfirm": "Изтриване на \"{{name}}\"? Това действие не може да бъде отменено.",
  "deleteConfirmButton": "Изтриване",
  "cancel": "Запази проекта",
  "newLeafletTitle": "Нова листовка",
  "tabBlank": "Празен",
  "tabTemplates": "Шаблони",
  "customWidthLabel": "Ширина (мм)",
  "customHeightLabel": "Височина (мм)",
  "useTemplate": "Използвай шаблона",
  "templateCategory": "Категория",
  "templateFormat": "Формат",
  "templatePages": "{{count}} страница",
  "templatePages_plural": "{{count}} страници",
  "pageCount": "{{count}} страница",
  "pageCount_plural": "{{count}} страници",
  "noThumbnail": "",
  "errorLoadProject": "Проектът не може да бъде отворен. Данните може да са повредени.",
  "errorDeleteProject": "Проектът не може да бъде изтрит. Опитайте отново.",
  "successDuplicate": "Проектът е дублиран."
}
```
  </action>
  <verify>
    <automated>grep -n "duplicateProject\|updateProjectName" src/lib/storage/projectStorage.ts && grep -c "dashboard" src/i18n/en.json && grep -c "dashboard" src/i18n/bg.json</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/storage/projectStorage.ts contains `export function duplicateProject(sourceId: string): string | null`
    - src/lib/storage/projectStorage.ts contains `export function updateProjectName(projectId: string, newName: string): boolean`
    - src/lib/storage/projectStorage.ts contains `Copy of ${source.meta.name}`
    - src/i18n/en.json contains `"dashboard"` key with `"newLeaflet": "New Leaflet"`
    - src/i18n/en.json contains `"deleteConfirm": "Delete \"{{name}}\"? This cannot be undone."`
    - src/i18n/en.json contains `"useTemplate": "Use This Template"`
    - src/i18n/bg.json contains `"dashboard"` key with `"newLeaflet": "Нова листовка"`
    - Existing functions (saveProject, loadProject, listProjects, deleteProject) are unchanged
  </acceptance_criteria>
  <done>projectStorage has duplicateProject and updateProjectName functions; en.json and bg.json both have complete dashboard namespace with all 28 string keys</done>
</task>

</tasks>

<verification>
- `grep -n "useAppStore" src/stores/appStore.ts` returns export line
- `grep -n "saveThumbnail\|getThumbnail\|deleteThumbnail" src/lib/storage/thumbnailDb.ts` returns 3 export lines
- `grep -n "duplicateProject\|updateProjectName" src/lib/storage/projectStorage.ts` returns 2 export lines
- `grep "dashboard.newLeaflet" src/i18n/en.json` returns match
- `npx jest --passWithNoTests` passes (no regressions)
</verification>

<success_criteria>
All 6 files created/updated. appStore provides view switching. thumbnailDb provides IndexedDB thumbnail CRUD. projectStorage has duplicate+rename. relativeTime formats dates. Both i18n files have complete dashboard strings. No existing tests broken.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-and-templates/05-01-SUMMARY.md`
</output>
