---
phase: 03-export
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - src/components/editor/modals/ExportModal.tsx
  - src/components/editor/ui/Header.tsx
  - src/stores/editorStore.ts
  - src/i18n/en.json
  - src/i18n/bg.json
autonomous: true
requirements:
  - EXPO-01
  - EXPO-02
  - EXPO-04
  - EXPO-05
  - EXPO-06
must_haves:
  truths:
    - "Export modal shows 4 format options: PNG/JPEG, InDesign, CorelDraw SVG, CorelDraw VBA"
    - "PNG/JPEG option shows DPI selector (72, 150, 300) and format toggle (PNG/JPEG)"
    - "All exports show progress/busy state during generation"
    - "Export button in Header opens the modal"
    - "Single page vs all pages option for raster and SVG exports"
  artifacts:
    - path: "src/components/editor/modals/ExportModal.tsx"
      provides: "Export modal with format picker and options"
---

<objective>
Create the Export modal UI that lets users choose format (PNG/JPEG, InDesign .jsx, CorelDraw SVG, CorelDraw VBA), configure options (DPI, single/all pages), and trigger the export.

Purpose: User-facing entry point to all export functions.
Output: Export modal component, Header integration, i18n strings.
</objective>

<context>
@.planning/phases/03-export/03-RESEARCH.md
@src/components/editor/modals/GenerateLeafletModal.tsx (modal pattern reference)
@src/components/editor/ui/Header.tsx
@src/stores/editorStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExportModal and wire into Header</name>
  <files>src/components/editor/modals/ExportModal.tsx, src/components/editor/ui/Header.tsx, src/stores/editorStore.ts, src/i18n/en.json, src/i18n/bg.json</files>
  <read_first>
    - src/components/editor/modals/GenerateLeafletModal.tsx (modal pattern)
    - src/components/editor/ui/Header.tsx (current export button)
    - src/stores/editorStore.ts (modal state pattern)
    - src/stores/canvasStore.ts (canvasRef, busyMessage)
    - src/stores/projectStore.ts (currentProject)
    - src/constants/formats.ts (format dimensions)
    - src/i18n/en.json
    - src/i18n/bg.json
  </read_first>
  <action>
1. Add `exportModalOpen` state + `setExportModalOpen` to editorStore (following generateModalOpen pattern)

2. Create `src/components/editor/modals/ExportModal.tsx`:
   - Modal backdrop + panel matching GenerateLeafletModal visual style (dark theme, same border/radius/shadow)
   - **Format cards** in a 2x2 grid:
     - PNG/JPEG (image icon) — "Raster images for web or print"
     - InDesign (.jsx) — "ExtendScript for Adobe InDesign"
     - CorelDraw SVG — "Vector files for CorelDraw"
     - CorelDraw VBA (.bas) — "Macro for CorelDraw automation"
   - Clicking a format card shows its options panel:
     - **PNG/JPEG**: DPI buttons (72/150/300), format toggle (PNG/JPEG), scope (current page / all pages)
     - **InDesign**: just an "Export" button (no extra options needed)
     - **CorelDraw SVG**: scope (current page / all pages)
     - **CorelDraw VBA**: just an "Export" button
   - Export button calls the corresponding function from the export modules
   - Show `busyMessage` via canvasStore during export (set before, clear after)
   - Close modal on successful export

3. Update Header.tsx:
   - Change the existing "Export JSON" button to open ExportModal instead
   - Keep JSON export available as an option inside the modal or as a separate small button

4. Add i18n strings for both en.json and bg.json:
   - export.title, export.raster, export.rasterDesc, export.indesign, export.indesignDesc
   - export.coreldrawSvg, export.coreldrawSvgDesc, export.coreldrawVba, export.coreldrawVbaDesc
   - export.dpi, export.format, export.scope, export.currentPage, export.allPages
   - export.exporting, export.success
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && npx tsc --noEmit 2>&1 | grep -i export | head -10</automated>
  </verify>
  <done>Export modal with 4 format options, DPI/scope controls, and Header integration complete.</done>
</task>

</tasks>
