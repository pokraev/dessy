---
phase: 02-editor-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editor/panels/LeftPanel.tsx
  - src/components/editor/panels/LayersPanel.tsx
  - src/components/editor/panels/PagesPanel.tsx
  - src/app/editor/page.tsx
autonomous: true
requirements:
  - LPNL-01
  - LPNL-02
  - LPNL-03
  - LPNL-04
  - LPNL-05
must_haves:
  truths:
    - "User can see 3 tabs (Tools, Layers, Pages) in the left panel and switch between them"
    - "User can see layer list with type icons, names, visibility/lock toggles, and drag-to-reorder"
    - "User can click a layer to select it on canvas and auto-switch to select tool"
    - "User can double-click a layer name to rename inline"
    - "User can see page thumbnails in Pages tab and click to navigate"
  artifacts:
    - path: "src/components/editor/panels/LeftPanel.tsx"
      provides: "Tabbed container for Tools, Layers, Pages"
      exports: ["LeftPanel"]
    - path: "src/components/editor/panels/LayersPanel.tsx"
      provides: "Layer list with drag reorder, visibility, lock"
      exports: ["LayersPanel"]
    - path: "src/components/editor/panels/PagesPanel.tsx"
      provides: "Page thumbnails with navigation"
      exports: ["PagesPanel"]
  key_links:
    - from: "src/components/editor/panels/LeftPanel.tsx"
      to: "src/components/editor/panels/ToolBar.tsx"
      via: "renders ToolBar as Tools tab content"
      pattern: "import.*ToolBar"
    - from: "src/app/editor/page.tsx"
      to: "src/components/editor/panels/LeftPanel.tsx"
      via: "EditorLayout leftPanel prop"
      pattern: "leftPanel=.*LeftPanel"
---

<objective>
Create the tabbed left panel (Tools | Layers | Pages) replacing the standalone ToolBar, with full layer management and page thumbnail navigation.

Purpose: The left panel is the primary workspace navigation. Tools tab preserves existing functionality, Layers tab gives z-order control, Pages tab gives multi-page navigation.
Output: 3 new components (LeftPanel, LayersPanel, PagesPanel), editor page updated to use LeftPanel.
</objective>

<execution_context>
@/Users/pokraev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pokraev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-editor-surface/02-CONTEXT.md
@.planning/phases/02-editor-surface/02-RESEARCH.md
@.planning/phases/02-editor-surface/02-UI-SPEC.md

<interfaces>
From src/components/editor/panels/ToolBar.tsx:
```typescript
export function ToolBar(): JSX.Element;
// Renders tool buttons (select, text, triangle, rect, circle, line, image, hand) + arrangement actions
// Width: 280px, bg-surface
```

From src/stores/canvasStore.ts:
```typescript
export type ToolId = 'select' | 'text' | 'triangle' | 'rect' | 'circle' | 'line' | 'image' | 'hand';
// canvasRef: Canvas | null; selectedObjectIds: string[]; setActiveTool(tool: ToolId): void;
// triggerSwitchPage: ((pageIndex: number) => void) | null;
```

From src/stores/projectStore.ts:
```typescript
// currentProject: Project | null; setCurrentProject(project: Project): void;
// setCurrentPageIndex(index: number): void; getCurrentPage(): Page | null;
```

From src/components/editor/panels/ContextMenu.tsx:
```typescript
// Existing right-click context menu component — reuse pattern for page context menu
```

From src/components/editor/EditorLayout.tsx:
```typescript
// leftPanel: React.ReactNode — slot prop for left panel content
```

From src/app/editor/page.tsx:
```typescript
// Currently: leftPanel={<ToolBar />}
// Must change to: leftPanel={<LeftPanel />}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LeftPanel tabbed container and LayersPanel with @dnd-kit</name>
  <files>src/components/editor/panels/LeftPanel.tsx, src/components/editor/panels/LayersPanel.tsx</files>
  <read_first>
    - src/components/editor/panels/ToolBar.tsx
    - src/components/editor/panels/ContextMenu.tsx
    - src/stores/canvasStore.ts
    - src/stores/projectStore.ts
    - .planning/phases/02-editor-surface/02-UI-SPEC.md
    - .planning/phases/02-editor-surface/02-RESEARCH.md
  </read_first>
  <action>
1. Create `src/components/editor/panels/LeftPanel.tsx`:
   - `'use client'` component
   - State: `activeTab: 'tools' | 'layers' | 'pages'` (default 'tools')
   - Tab bar: 36px height, bg `#141414` (`bg-surface`), 3 equal-width tabs
   - Active tab: bottom 2px border `#6366f1`, text `#f5f5f5` weight 600, font-size 11px uppercase letter-spacing 0.08em
   - Inactive tab: text `#888888` weight 400, hover text `#f5f5f5`
   - Tab labels: "Tools", "Layers", "Pages" (exact copy from UI-SPEC)
   - Tab content area: `flex-1 overflow-y-auto`
   - Conditionally render: `activeTab === 'tools'` → `<ToolBar />`, `'layers'` → `<LayersPanel />`, `'pages'` → `<PagesPanel />`
   - Import ToolBar from same directory (existing component)
   - Full height layout: flex column, tab bar flex-shrink-0, content flex-1

2. Create `src/components/editor/panels/LayersPanel.tsx`:
   - `'use client'` component
   - Import `DndContext`, `closestCenter`, `DragEndEvent` from `@dnd-kit/core`
   - Import `SortableContext`, `verticalListSortingStrategy`, `useSortable` from `@dnd-kit/sortable`
   - Import `CSS` from `@dnd-kit/utilities`
   - Import `useCanvasLayers`, `LayerItem` from `@/hooks/useCanvasLayers`
   - Import lucide icons: `GripVertical`, `Type`, `Image`, `Square`, `RectangleHorizontal`, `Eye`, `EyeOff`, `Lock`, `Unlock`

   LayersPanel component:
   - Call `useCanvasLayers()` to get `{ layers, moveLayer, toggleVisibility, toggleLock, renameLayer, selectLayer }`
   - Get `selectedObjectIds` from canvasStore
   - If layers.length === 0, show empty state: "No layers yet" heading (13px, `#f5f5f5`) + "Add elements to the canvas to see them here." body (11px, `#888888`)
   - Otherwise render DndContext + SortableContext with `verticalListSortingStrategy`
   - Items array: `layers.map(l => l.id)`
   - `onDragEnd`: extract `active.id` and `over.id`, find indices in layers array, call `moveLayer(fromIndex, toIndex)`

   LayerRow component (inside same file):
   - Props: `{ layer: LayerItem; isSelected: boolean; onSelect, onToggleVisibility, onToggleLock, onRename }`
   - Use `useSortable({ id: layer.id })` for DnD
   - Height: 32px, horizontal padding 8px
   - Layout: `[drag handle 16px] [type icon 16px] [name text flex-1] [eye icon 20px] [lock icon 20px]`
   - Drag handle: `GripVertical` icon, color `#555555`, visible on row hover only (opacity 0 by default, 1 on hover via CSS)
   - `...listeners` and `...attributes` on drag handle span only (not entire row)
   - Type icons by `layer.type`: `'text'` → `Type`, `'image'` → `Image`, `'shape'` → `Square`, `'colorBlock'` → `RectangleHorizontal` — all 16px, color `#888888`
   - Name text: 13px, `#f5f5f5`, flex-1, overflow hidden, text-overflow ellipsis, white-space nowrap
   - Row selected: bg `#1e1e1e`
   - Row hover: bg `rgba(30,30,30,0.6)` (bg-surface-raised at 60%)
   - Click row (not on eye/lock icons): call `onSelect(layer.id)`
   - Eye icon: `Eye` when `layer.visible` / `EyeOff` when not — color `#888888`, hover `#f5f5f5`
     - `aria-label`: `"Toggle visibility for {layer.name}"`
     - Click: `onToggleVisibility(layer.id)`
   - Lock icon: `Lock` when `layer.locked` / `Unlock` when not — color `#888888`
     - `aria-label`: `"Toggle lock for {layer.name}"`
     - Click: `onToggleLock(layer.id)`
   - Double-click on name text: enter rename mode
     - Rename mode: replace text span with an `<input>` element, value is current name, border-bottom `1px solid #6366f1`, bg transparent
     - On Enter or blur: commit rename via `onRename(layer.id, newName)`, exit rename mode
     - On Escape: revert and exit rename mode
   - Apply `transform` and `transition` from useSortable via `CSS.Transform.toString(transform)`
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/editor/panels/LeftPanel.tsx contains activeTab state with 'tools', 'layers', 'pages' values
    - src/components/editor/panels/LeftPanel.tsx renders ToolBar component when activeTab is 'tools'
    - src/components/editor/panels/LeftPanel.tsx tab bar has height 36px
    - src/components/editor/panels/LayersPanel.tsx imports from @dnd-kit/core and @dnd-kit/sortable
    - src/components/editor/panels/LayersPanel.tsx contains DndContext and SortableContext
    - src/components/editor/panels/LayersPanel.tsx contains GripVertical icon
    - src/components/editor/panels/LayersPanel.tsx contains "No layers yet" empty state text
    - src/components/editor/panels/LayersPanel.tsx contains "Toggle visibility for" aria-label
    - src/components/editor/panels/LayersPanel.tsx contains "Toggle lock for" aria-label
    - src/components/editor/panels/LayersPanel.tsx contains double-click rename logic with input element
    - npx next build succeeds
  </acceptance_criteria>
  <done>LeftPanel renders 3 tabs with correct styling, LayersPanel shows layer list with drag-to-reorder via @dnd-kit, visibility/lock toggles, click-to-select, double-click-to-rename</done>
</task>

<task type="auto">
  <name>Task 2: Create PagesPanel with thumbnails and wire LeftPanel into editor page</name>
  <files>src/components/editor/panels/PagesPanel.tsx, src/app/editor/page.tsx</files>
  <read_first>
    - src/app/editor/page.tsx
    - src/stores/projectStore.ts
    - src/stores/canvasStore.ts
    - src/components/editor/ui/BottomBar.tsx
    - .planning/phases/02-editor-surface/02-UI-SPEC.md
    - .planning/phases/02-editor-surface/02-RESEARCH.md
  </read_first>
  <action>
1. Create `src/components/editor/panels/PagesPanel.tsx`:
   - `'use client'` component
   - Import `DndContext`, `closestCenter`, `DragEndEvent` from `@dnd-kit/core`
   - Import `SortableContext`, `verticalListSortingStrategy`, `useSortable` from `@dnd-kit/sortable`
   - Import `CSS` from `@dnd-kit/utilities`
   - Import `Plus` from `lucide-react`

   State and data:
   - Get `currentProject` from projectStore, `triggerSwitchPage` from canvasStore
   - `thumbnails` state: `Record<number, string>` mapping page index to data URL
   - Generate thumbnail for current page when Pages tab mounts or when `currentProject.currentPageIndex` changes:
     ```typescript
     const canvas = useCanvasStore.getState().canvasRef;
     if (canvas) {
       const dataUrl = canvas.toDataURL({ multiplier: 0.15, format: 'jpeg', quality: 0.6 });
       setThumbnails(prev => ({ ...prev, [currentProject.currentPageIndex]: dataUrl }));
     }
     ```
   - Lazy thumbnail generation: only capture when PagesPanel mounts (not eagerly for all pages)

   Page list:
   - Render pages using SortableContext for drag-to-reorder
   - Each PageThumbnail: `useSortable({ id: page.id })`
   - Thumbnail area: 64px height, aspect-ratio from document format (use `FORMATS[currentProject.meta.format]` to calculate ratio), bg `#1e1e1e`, border `1px solid #2a2a2a`
   - If thumbnail data URL exists for that page index, render as `<img>` filling the thumbnail area with `object-fit: contain`
   - If no thumbnail, show page number centered in gray text
   - Active page: border-color `#6366f1`, plus subtle tint bg `#1a1a2e`
   - Below thumbnail: page number label 11px, color `#888888` (e.g., "Page 1")
   - Click thumbnail: call `triggerSwitchPage(pageIndex)` — save current page thumbnail first

   Right-click context menu on thumbnail (reuse ContextMenu pattern from existing code):
   - "Duplicate Page" — duplicate this page's sessionStorage JSON to a new page index, add page to projectStore, switch to it
   - "Delete Page" — show confirmation "Delete this page? This cannot be undone." with "Keep Page" (dismiss) and "Delete Page" (destructive, bg `#ef4444`)
     - Only enabled if pages.length > 1 (cannot delete last page)
     - On delete: remove page from projectStore.pages, remove sessionStorage key, update indices

   Drag-to-reorder:
   - On DragEnd: reorder `projectStore.pages` array AND remap all sessionStorage keys:
     1. Read all page JSONs into temp array
     2. Reorder projectStore.pages
     3. Re-write all sessionStorage keys in new order: `dessy-generated-page-{projectId}-{newIndex}`

   "+ Add Page" button at bottom:
   - Full width, height 36px, bg `#1e1e1e`, border `1px dashed #2a2a2a`, border-radius 4px
   - Text "Add Page" with Plus icon, 11px, color `#888888`, hover color `#f5f5f5`
   - On click: add new page `{ id: crypto.randomUUID(), elements: [], background: '#FFFFFF' }` to projectStore.pages, switch to it via triggerSwitchPage

2. Update `src/app/editor/page.tsx`:
   - Replace `import { ToolBar } from '@/components/editor/panels/ToolBar'` with `import { LeftPanel } from '@/components/editor/panels/LeftPanel'`
   - Change `leftPanel={<ToolBar />}` to `leftPanel={<LeftPanel />}`
   - Keep all other imports and logic unchanged
   - Also: when initializing project (both saved and new), ensure `brandSwatches: []` and `typographyPresets: []` are included in the project object (new fields from Plan 01)
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/editor/panels/PagesPanel.tsx imports DndContext from @dnd-kit/core
    - src/components/editor/panels/PagesPanel.tsx contains toDataURL for thumbnail generation
    - src/components/editor/panels/PagesPanel.tsx contains "Add Page" button text
    - src/components/editor/panels/PagesPanel.tsx contains "Delete this page" confirmation text
    - src/components/editor/panels/PagesPanel.tsx contains "Duplicate Page" text
    - src/components/editor/panels/PagesPanel.tsx contains sessionStorage key remap logic on reorder
    - src/app/editor/page.tsx imports LeftPanel (not ToolBar for the leftPanel prop)
    - src/app/editor/page.tsx contains leftPanel={<LeftPanel />} (or equivalent JSX)
    - src/app/editor/page.tsx contains brandSwatches and typographyPresets in project initialization
    - npx next build succeeds
  </acceptance_criteria>
  <done>PagesPanel shows page thumbnails with click-to-navigate, right-click context menu for duplicate/delete, drag-to-reorder with sessionStorage key remap, Add Page button. Editor page wired to use LeftPanel instead of standalone ToolBar.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- `grep -r "LeftPanel" src/app/editor/page.tsx` returns match
- `grep -r "DndContext" src/components/editor/panels/LayersPanel.tsx` returns match
- `grep -r "DndContext" src/components/editor/panels/PagesPanel.tsx` returns match
- `grep -r "triggerSwitchPage" src/components/editor/panels/PagesPanel.tsx` returns match
</verification>

<success_criteria>
Left panel has 3 working tabs. Tools tab shows existing ToolBar. Layers tab shows canvas objects with drag reorder and visibility/lock controls. Pages tab shows thumbnails with navigation and CRUD operations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-editor-surface/02-02-SUMMARY.md`
</output>
