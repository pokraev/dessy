---
phase: 01.1-import
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/editor/modals/GenerateLeafletModal.tsx
  - src/components/editor/modals/tabs/PromptTab.tsx
  - src/components/editor/modals/tabs/PhotoTab.tsx
  - src/components/editor/modals/tabs/SketchTab.tsx
  - src/components/editor/modals/FoldTypePicker.tsx
  - src/components/editor/modals/StylePicker.tsx
  - src/components/editor/modals/GenerationPreview.tsx
autonomous: true
requirements:
  - AIGEN-04
  - AIGEN-05
  - AIGEN-06

must_haves:
  truths:
    - "User can open AI generation modal from the header"
    - "User can type a text prompt and select a fold type"
    - "User can upload a photo of an existing leaflet"
    - "User can upload a sketch image"
    - "User sees loading state during generation"
    - "User sees a preview of generated result"
    - "User can regenerate or load result into editor"
  artifacts:
    - path: "src/components/editor/modals/GenerateLeafletModal.tsx"
      provides: "Modal wrapper with 3 tab navigation"
      min_lines: 60
    - path: "src/components/editor/modals/tabs/PromptTab.tsx"
      provides: "Text prompt input with fold type and style pickers"
      min_lines: 40
    - path: "src/components/editor/modals/tabs/PhotoTab.tsx"
      provides: "Photo upload with preview and fold type picker"
      min_lines: 40
    - path: "src/components/editor/modals/tabs/SketchTab.tsx"
      provides: "Sketch upload with preview and fold type picker"
      min_lines: 40
    - path: "src/components/editor/modals/FoldTypePicker.tsx"
      provides: "Visual fold type selector with diagrams"
      min_lines: 30
    - path: "src/components/editor/modals/GenerationPreview.tsx"
      provides: "Preview of generated canvas pages"
      min_lines: 30
  key_links:
    - from: "src/components/editor/modals/GenerateLeafletModal.tsx"
      to: "src/types/generation.ts"
      via: "imports GenerationRequest, GenerationResponse"
      pattern: "import.*generation"
    - from: "src/components/editor/modals/GenerateLeafletModal.tsx"
      to: "/api/generate-leaflet"
      via: "fetch POST call"
      pattern: "fetch.*generate-leaflet"
---

<objective>
Build the AI generation modal UI with 3 tabs (Prompt, Photo, Sketch), fold type picker, style picker, loading states, and generation preview.

Purpose: Users need a polished, intuitive interface to interact with the AI generation backend. This modal is the primary entry point for the AI leaflet generation feature.

Output: Complete modal component tree that can call the API and display results.
</objective>

<execution_context>
@/Users/pokraev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pokraev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-import/01.1-CONTEXT.md

<interfaces>
<!-- Existing UI patterns the executor must match -->

From src/components/editor/ui/Header.tsx:
```typescript
// Header uses: bg-surface, border-border, text-text-primary, accent #6366f1
// Button height: 32px, border-radius: 8px, font-size: 13px
// Pattern: triggerFn callbacks stored in canvasStore, called by Header
```

From src/stores/editorStore.ts:
```typescript
interface EditorState {
  showGrid: boolean;
  showRulers: boolean;
  showGuides: boolean;
  leftPanelOpen: boolean;
  rightPanelOpen: boolean;
  shortcutsModalOpen: boolean;
  // ... toggle functions
}
```

From src/types/generation.ts (created in Plan 01):
```typescript
export type GenerationMode = 'prompt' | 'photo' | 'sketch';
export type FoldType = 'single' | 'bifold' | 'trifold' | 'zfold';
export interface GenerationRequest { mode, foldType, prompt?, imageBase64?, brandColors?, style?, logoBase64? }
export interface GenerationResponse { pages: GeneratedPage[], formatId, suggestedName }
export interface GeneratedPage { canvasJSON: Record<string, unknown>, pageLabel: string }
```

From src/stores/brandStore.ts:
```typescript
export const useBrandStore = create<BrandState>((set) => ({
  brandColors: [],  // ColorSwatch[]
  typographyPresets: [],  // TypographyPreset[]
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FoldTypePicker, StylePicker, and GenerationPreview shared components</name>
  <read_first>
    - src/components/editor/ui/Header.tsx
    - src/components/editor/ui/KeyboardShortcutsModal.tsx
    - src/types/generation.ts
    - src/stores/editorStore.ts
  </read_first>
  <files>
    src/components/editor/modals/FoldTypePicker.tsx,
    src/components/editor/modals/StylePicker.tsx,
    src/components/editor/modals/GenerationPreview.tsx
  </files>
  <action>
**1. Create `src/components/editor/modals/FoldTypePicker.tsx`:**

'use client' component. Props: `{ value: FoldType; onChange: (ft: FoldType) => void }`.

Render 4 clickable cards in a 2x2 grid for fold types:
- **Single** — icon: single rectangle. Label: "Single Page". Subtitle: "A4 - 1 page"
- **Bi-fold** — icon: rectangle with center fold line. Label: "Bi-fold". Subtitle: "4 panels"
- **Tri-fold** — icon: rectangle with 2 fold lines. Label: "Tri-fold". Subtitle: "6 panels"
- **Z-fold** — icon: zigzag fold shape. Label: "Z-fold". Subtitle: "6 panels"

Each card: `bg-surface-raised` when unselected, `border-2 border-accent` when selected (`value === foldType`). Height: 80px. Border radius: 8px. Cursor pointer. Use simple SVG icons (inline, ~40x30px) showing fold lines as dashed strokes on a white rectangle.

Dark theme colors: bg `#1e1e1e`, selected border `#6366f1`, text `#f5f5f5`, subtitle `#888888`. Font: 12px labels, 11px subtitle.

**2. Create `src/components/editor/modals/StylePicker.tsx`:**

'use client' component. Props: `{ value: string; onChange: (style: string) => void }`.

Render a row of 5 pill buttons for styles: 'minimal', 'bold', 'corporate', 'playful', 'elegant'.

Each pill: `px-3 py-1.5`, rounded-full, font-size 12px. Unselected: `bg-surface-raised text-text-secondary`. Selected: `bg-accent text-white`. Gap between pills: 8px. Flex row, flex-wrap.

**3. Create `src/components/editor/modals/GenerationPreview.tsx`:**

'use client' component. Props: `{ pages: GeneratedPage[]; isLoading: boolean; error: string | null; onRegenerate: () => void; onLoadIntoEditor: () => void }`.

Layout:
- If `isLoading`: show a centered spinner (use a simple CSS animation div, 40x40px, border-4 border-accent border-t-transparent, rounded-full, animate-spin) with text "Generating your leaflet..." below in text-secondary.
- If `error`: show error text in `text-danger` (#ef4444) with a "Try Again" button.
- If `pages` has content: show page previews as cards in a horizontal scroll container. Each card shows the `pageLabel` and a placeholder representation (gray rectangle with "Page N" text, since actual canvas rendering happens at load time). Below the preview cards, show two buttons side by side:
  - "Load into Editor" — primary button (bg-accent, text-white, 13px, h-36px, rounded-lg, px-4)
  - "Regenerate" — secondary button (border border-border, text-text-primary, same sizing)
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && npx tsc --noEmit src/components/editor/modals/FoldTypePicker.tsx src/components/editor/modals/StylePicker.tsx src/components/editor/modals/GenerationPreview.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "FoldType" src/components/editor/modals/FoldTypePicker.tsx
    - grep -q "single.*bifold.*trifold.*zfold" src/components/editor/modals/FoldTypePicker.tsx
    - grep -q "minimal.*bold.*corporate" src/components/editor/modals/StylePicker.tsx
    - grep -q "GeneratedPage" src/components/editor/modals/GenerationPreview.tsx
    - grep -q "isLoading" src/components/editor/modals/GenerationPreview.tsx
    - grep -q "Load into Editor" src/components/editor/modals/GenerationPreview.tsx
    - grep -q "Regenerate" src/components/editor/modals/GenerationPreview.tsx
  </acceptance_criteria>
  <done>
    - FoldTypePicker renders 4 selectable fold type cards with visual diagrams
    - StylePicker renders 5 style pill buttons with selection state
    - GenerationPreview handles loading, error, and success states with action buttons
    - All components use dark theme colors matching existing editor UI
  </done>
</task>

<task type="auto">
  <name>Task 2: Tab components and GenerateLeafletModal with Header integration</name>
  <read_first>
    - src/components/editor/modals/FoldTypePicker.tsx (just created)
    - src/components/editor/modals/StylePicker.tsx (just created)
    - src/components/editor/modals/GenerationPreview.tsx (just created)
    - src/components/editor/ui/Header.tsx
    - src/components/editor/ui/KeyboardShortcutsModal.tsx
    - src/stores/editorStore.ts
    - src/stores/brandStore.ts
    - src/types/generation.ts
  </read_first>
  <files>
    src/components/editor/modals/tabs/PromptTab.tsx,
    src/components/editor/modals/tabs/PhotoTab.tsx,
    src/components/editor/modals/tabs/SketchTab.tsx,
    src/components/editor/modals/GenerateLeafletModal.tsx,
    src/components/editor/ui/Header.tsx,
    src/stores/editorStore.ts
  </files>
  <action>
**1. Create `src/components/editor/modals/tabs/PromptTab.tsx`:**

'use client' component. Props: `{ foldType: FoldType; onFoldTypeChange: (ft: FoldType) => void; style: string; onStyleChange: (s: string) => void; onGenerate: (prompt: string) => void; isGenerating: boolean }`.

Layout (vertical stack, gap-4):
- Label "Describe your leaflet" (11px, uppercase, tracking-wide, text-secondary)
- Textarea: 4 rows, placeholder "e.g. Tri-fold leaflet for a yoga studio, calm and minimal design with earth tones", bg-surface-raised, border border-border, rounded-lg, text-text-primary, p-3, font-size 13px, resize-none, w-full. On focus: border-accent.
- FoldTypePicker (imported)
- StylePicker (imported)
- "Generate" button: full width, bg-accent, text-white, h-40px, rounded-lg, font-medium, 14px. Disabled when textarea empty or isGenerating. When disabled: opacity-50 cursor-not-allowed. Use Sparkles icon from lucide-react (size 16) before text.

Manage textarea state locally with useState. On "Generate" click: call `onGenerate(textareaValue)`.

**2. Create `src/components/editor/modals/tabs/PhotoTab.tsx`:**

'use client' component. Props: `{ foldType: FoldType; onFoldTypeChange: (ft: FoldType) => void; style: string; onStyleChange: (s: string) => void; onGenerate: (imageBase64: string) => void; isGenerating: boolean }`.

Layout (vertical stack, gap-4):
- Label "Upload a photo of an existing leaflet" (11px, uppercase, tracking-wide, text-secondary)
- Drop zone: dashed border (border-2 border-dashed border-border), rounded-lg, h-48, flex center, cursor-pointer. Shows Upload icon (lucide-react, 32px, text-secondary) and "Click or drag to upload" (13px, text-secondary). On file drop or click: read file as base64, show preview image inside the drop zone (object-contain, max-h-44).
- Hidden `<input type="file" accept="image/*">` triggered by drop zone click.
- Note text: "The AI will analyze the layout structure and create an editable version with placeholder text" (11px, text-secondary, italic)
- FoldTypePicker
- StylePicker
- "Generate from Photo" button: same styling as PromptTab Generate button. Disabled when no image uploaded or isGenerating.

Manage image state locally: `imageBase64: string | null`, `imagePreview: string | null`.

**3. Create `src/components/editor/modals/tabs/SketchTab.tsx`:**

'use client' component. Nearly identical to PhotoTab but with different labels:
- Label: "Upload a photo of your hand-drawn sketch"
- Drop zone text: "Click or drag to upload your sketch"
- Note: "The AI will interpret boxes as content zones, scribbles as text, and blobs as image placeholders"
- Button: "Generate from Sketch"

**4. Create `src/components/editor/modals/GenerateLeafletModal.tsx`:**

'use client' component. Props: `{ open: boolean; onClose: () => void; onLoadPages: (response: GenerationResponse) => void }`.

This is the main modal. Layout:
- Backdrop: fixed inset-0, bg-black/60, z-50, flex items-center justify-center. Click backdrop to close.
- Modal container: bg-surface (#141414), border border-border, rounded-xl, w-[640px], max-h-[90vh], overflow-y-auto, shadow-2xl.
- Header row: flex justify-between items-center, p-4, border-b border-border. Title "AI Leaflet Generator" (16px, font-semibold, text-text-primary). Close button (X icon, lucide-react, 18px).
- Tab bar: flex, border-b border-border. 3 tabs: "Prompt" (Sparkles icon), "Photo" (Camera icon), "Sketch" (Pencil icon). Each tab: px-4 py-2.5, font-size 13px, text-secondary, cursor-pointer, border-b-2 border-transparent. Active tab: text-accent, border-b-2 border-accent. Icons from lucide-react, 14px, mr-1.5.
- Tab content: p-4. Render PromptTab, PhotoTab, or SketchTab based on active tab.
- GenerationPreview: shown below tab content when generation has been triggered (response or loading or error).

State management (local React state — per CONTEXT.md Claude's Discretion):
- `activeTab: GenerationMode` (default 'prompt')
- `foldType: FoldType` (default 'single')
- `style: string` (default 'minimal')
- `isGenerating: boolean`
- `response: GenerationResponse | null`
- `error: string | null`

Generation flow:
- Tab calls onGenerate with mode-specific data
- Modal sets isGenerating=true, error=null
- POST to `/api/generate-leaflet` with body: `{ mode: activeTab, foldType, prompt (or imageBase64), brandColors: useBrandStore.getState().brandColors.map(c => c.hex), typographyPresets: useBrandStore.getState().typographyPresets, style, logoBase64 }`
- On success: set response, isGenerating=false
- On error: set error message, isGenerating=false

Wrap modal content in `AnimatePresence` + `motion.div` (from 'motion/react') for fade-in/scale animation matching existing KeyboardShortcutsModal pattern. initial={{ opacity: 0, scale: 0.95 }}, animate={{ opacity: 1, scale: 1 }}, exit={{ opacity: 0, scale: 0.95 }}.

**5. Update `src/stores/editorStore.ts`:**

Add `generateModalOpen: boolean` to EditorState interface and store. Default: false.
Add `setGenerateModalOpen: (open: boolean) => void` action.

DO NOT modify any existing fields or functions — only add the new ones.

**6. Update `src/components/editor/ui/Header.tsx`:**

Add an "AI Generate" button to the left side of the header (after the project name, before the center undo/redo section). Style:
- flex items-center gap-1.5
- height 32px, px-3, rounded-lg, font-size 13px
- bg-gradient: `background: linear-gradient(135deg, #6366f1, #8b5cf6)` (indigo to violet gradient for the AI button to distinguish it)
- text-white, font-medium
- Sparkles icon (lucide-react, 14px) before text "AI Generate"
- On hover: brightness-110
- onClick: `useEditorStore.getState().setGenerateModalOpen(true)`

Also import and render `GenerateLeafletModal` in Header:
```tsx
const generateModalOpen = useEditorStore((s) => s.generateModalOpen);
// ... in JSX:
<GenerateLeafletModal
  open={generateModalOpen}
  onClose={() => useEditorStore.getState().setGenerateModalOpen(false)}
  onLoadPages={(response) => {
    useEditorStore.getState().setGenerateModalOpen(false);
    // Loading into canvas happens in Plan 03
  }}
/>
```

The `onLoadPages` callback will be wired to actual canvas loading in Plan 03. For now it just closes the modal.
  </action>
  <verify>
    <automated>cd /Users/pokraev/Projects/Dessy && npx tsc --noEmit src/components/editor/modals/GenerateLeafletModal.tsx src/components/editor/ui/Header.tsx 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PromptTab" src/components/editor/modals/GenerateLeafletModal.tsx
    - grep -q "PhotoTab" src/components/editor/modals/GenerateLeafletModal.tsx
    - grep -q "SketchTab" src/components/editor/modals/GenerateLeafletModal.tsx
    - grep -q "GenerationPreview" src/components/editor/modals/GenerateLeafletModal.tsx
    - grep -q "api/generate-leaflet" src/components/editor/modals/GenerateLeafletModal.tsx
    - grep -q "AnimatePresence" src/components/editor/modals/GenerateLeafletModal.tsx
    - grep -q "generateModalOpen" src/stores/editorStore.ts
    - grep -q "AI Generate" src/components/editor/ui/Header.tsx
    - grep -q "GenerateLeafletModal" src/components/editor/ui/Header.tsx
    - grep -q "Sparkles" src/components/editor/ui/Header.tsx
    - grep -q "onGenerate" src/components/editor/modals/tabs/PromptTab.tsx
    - grep -q "imageBase64" src/components/editor/modals/tabs/PhotoTab.tsx
    - grep -q "sketch" src/components/editor/modals/tabs/SketchTab.tsx
  </acceptance_criteria>
  <done>
    - GenerateLeafletModal renders with 3 tabs, each tab has fold type + style pickers
    - Modal calls /api/generate-leaflet and displays result in GenerationPreview
    - Header has "AI Generate" button that opens the modal
    - editorStore has generateModalOpen state
    - All components use dark theme colors matching existing editor UI
    - Photo and Sketch tabs accept image uploads as base64
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new and modified files
- Header renders AI Generate button
- Modal opens/closes via editorStore toggle
- All three tabs render with appropriate inputs
- Generation preview shows loading/error/success states
</verification>

<success_criteria>
- User can click "AI Generate" in the header to open the modal
- Modal has 3 functional tabs with fold type and style pickers
- Prompt tab accepts text input, Photo/Sketch tabs accept image upload
- Generate button calls API and shows loading state
- Preview shows result with "Load into Editor" and "Regenerate" buttons
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-import/01.1-02-SUMMARY.md`
</output>
